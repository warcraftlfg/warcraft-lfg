GET /messages/[ObjId1]/[ObjId2]
--> Check if user is owner of one of ObjId1 or ObjId2
--> Get all message with ObjId1 & ObjId2 inside

GET /messages
--> Get all guild character and userid And return it to people.

POST /messages
--> Check if user is member of all objectId
--> {from:ObjectId, to:ObjectId , text:text, userId}